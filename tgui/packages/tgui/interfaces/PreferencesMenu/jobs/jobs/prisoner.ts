import { Job } from "../base";
import { Command } from "../departments";

const Prisoner: Job = {
  name: "Prisoner",
  description: "Keep yourself occupied while in permabrig.",
  department: Command,
};

export default Prisoner;
